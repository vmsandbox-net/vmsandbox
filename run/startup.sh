#!/bin/sh
# Shell startup script — sourced by bash when a login shell starts.
# Place shell setup here: TERM, aliases, PS1, tab name, app start, etc.
export TERM=xterm-256color
alias ls='ls --color=auto'
alias ll='ls -alF'
